---
const { className, SectionName, fullWidth = false } = Astro.props;
---

<section
  class={`${className} ${fullWidth ? "w-full" : "container mx-auto px-4 lg:max-w-[1024px]"}`}
  id={SectionName}
>
  <slot />
</section>
